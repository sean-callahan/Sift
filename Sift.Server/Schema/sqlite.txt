CREATE TABLE IF NOT EXISTS users (id INTEGER PRIMARY KEY AUTOINCREMENT, username VARCHAR(64) NOT NULL UNIQUE, password CHAR(60) NOT NULL);
CREATE TABLE IF NOT EXISTS settings (`key` VARCHAR(32) PRIMARY KEY, `category` VARCHAR(32), `value` TEXT);